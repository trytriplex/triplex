---
"@triplex/server": minor
"triplex-vsce": minor
---

Triplex now respects the name prop set on custom components in the Scene Panel. E.g. given a component Box, if it has a name prop statically set to "foo" it will appear as "foo (Box)" in the Scene Panel.
